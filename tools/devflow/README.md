# 已移至 `devflow-kit/`

本 repo 之前的 Devflow 實作在 `tools/devflow/`，現在已抽離成可獨立搬移的通用工具：

- `devflow-kit/`

請改用：

- 文件：`devflow-kit/docs/DEVFLOW.md`
- CLI：`node devflow-kit/bin/devflow.js ...`
- Server/UI：`node devflow-kit/bin/devflow.js server --port 4170`

